spring:
  application:
    name: api-gateway
  # Add spring.config.import property to resolve Nacos configuration error
  config:
    import: "optional:nacos:"
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: DEFAULT_GROUP