spring:
  application:
    name: eureka-server
  # Add spring.config.import property for Nacos configuration
  config:
    import: "optional:nacos:"
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: nacos-server:8848
        file-extension: yml
        group: DEFAULT_GROUP